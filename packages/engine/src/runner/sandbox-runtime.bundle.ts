/**
 * Auto-generated sandbox runtime bundle
 * 
 * DO NOT EDIT - This file is generated by scripts/bundle-sandbox.ts
 * 
 * Run 'pnpm run bundle-sandbox' to regenerate
 */

/**
 * The bundled sandbox runtime code as a string.
 * This code runs inside the vm or isolated-vm context.
 * 
 * Before running, the host must set _playerId global:
 * ```
 * context.global._playerId = 'player1';
 * ```
 */
export const sandboxRuntimeCode = "\"use strict\";\n(() => {\n  var __defProp = Object.defineProperty;\n  var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;\n  var __publicField = (obj, key, value) => {\n    __defNormalProp(obj, typeof key !== \"symbol\" ? key + \"\" : key, value);\n    return value;\n  };\n\n  // ../types/dist/constants.js\n  var OK = 0;\n  var ERR_NOT_OWNER = -1;\n  var ERR_NO_PATH = -2;\n  var ERR_NAME_EXISTS = -3;\n  var ERR_BUSY = -4;\n  var ERR_NOT_FOUND = -5;\n  var ERR_NOT_ENOUGH_ENERGY = -6;\n  var ERR_NOT_ENOUGH_RESOURCES = -6;\n  var ERR_INVALID_TARGET = -7;\n  var ERR_FULL = -8;\n  var ERR_NOT_IN_RANGE = -9;\n  var ERR_INVALID_ARGS = -10;\n  var ERR_TIRED = -11;\n  var ERR_NO_BODYPART = -12;\n  var ERR_NOT_ENOUGH_EXTENSIONS = -6;\n  var MOVE = \"move\";\n  var RANGED_ATTACK = \"ranged_attack\";\n  var HEAL = \"heal\";\n  var ATTACK = \"attack\";\n  var CARRY = \"carry\";\n  var TOUGH = \"tough\";\n  var WORK = \"work\";\n  var TOP = 1;\n  var TOP_RIGHT = 2;\n  var RIGHT = 3;\n  var BOTTOM_RIGHT = 4;\n  var BOTTOM = 5;\n  var BOTTOM_LEFT = 6;\n  var LEFT = 7;\n  var TOP_LEFT = 8;\n  var TERRAIN_PLAIN = 0;\n  var TERRAIN_WALL = 1;\n  var TERRAIN_SWAMP = 2;\n  var STRUCTURE_SPAWN = \"spawn\";\n  var STRUCTURE_EXTENSION = \"extension\";\n  var STRUCTURE_ROAD = \"road\";\n  var STRUCTURE_WALL = \"constructedWall\";\n  var STRUCTURE_RAMPART = \"rampart\";\n  var STRUCTURE_KEEPER_LAIR = \"keeperLair\";\n  var STRUCTURE_PORTAL = \"portal\";\n  var STRUCTURE_CONTROLLER = \"controller\";\n  var STRUCTURE_LINK = \"link\";\n  var STRUCTURE_STORAGE = \"storage\";\n  var STRUCTURE_TOWER = \"tower\";\n  var STRUCTURE_OBSERVER = \"observer\";\n  var STRUCTURE_POWER_BANK = \"powerBank\";\n  var STRUCTURE_POWER_SPAWN = \"powerSpawn\";\n  var STRUCTURE_EXTRACTOR = \"extractor\";\n  var STRUCTURE_LAB = \"lab\";\n  var STRUCTURE_TERMINAL = \"terminal\";\n  var STRUCTURE_CONTAINER = \"container\";\n  var STRUCTURE_NUKER = \"nuker\";\n  var STRUCTURE_FACTORY = \"factory\";\n  var STRUCTURE_INVADER_CORE = \"invaderCore\";\n  var STRUCTURE_CREEP = \"creep\";\n  var STRUCTURE_CONSTRUCTED_WALL = \"constructedWall\";\n  var SOURCE = \"source\";\n  var RESOURCE = \"resource\";\n  var CONSTRUCTION_SITE = \"constructionSite\";\n  var BODYPART_HITS = 100;\n  var RANGED_ATTACK_POWER = 10;\n  var RANGED_ATTACK_DISTANCE_RATE = {\n    1: 1,\n    2: 0.4,\n    3: 0.1\n  };\n  var ATTACK_POWER = 30;\n  var HEAL_POWER = 12;\n  var RANGED_HEAL_POWER = 4;\n  var CARRY_CAPACITY = 50;\n  var HARVEST_POWER = 2;\n  var BUILD_POWER = 5;\n  var OBSTACLE_OBJECT_TYPES = [\n    \"spawn\",\n    \"creep\",\n    \"wall\",\n    \"source\",\n    \"tower\",\n    \"extension\",\n    \"container\"\n  ];\n  var TOWER_ENERGY_COST = 10;\n  var TOWER_RANGE = 50;\n  var TOWER_HITS = 3e3;\n  var TOWER_CAPACITY = 50;\n  var TOWER_POWER_ATTACK = 150;\n  var TOWER_POWER_HEAL = 100;\n  var TOWER_OPTIMAL_RANGE = 5;\n  var TOWER_FALLOFF_RANGE = 20;\n  var TOWER_FALLOFF = 0.75;\n  var TOWER_COOLDOWN = 10;\n  var BODYPART_COST = {\n    [MOVE]: 50,\n    [WORK]: 100,\n    [CARRY]: 50,\n    [ATTACK]: 80,\n    [RANGED_ATTACK]: 150,\n    [HEAL]: 250,\n    [TOUGH]: 10\n  };\n  var MAX_CREEP_SIZE = 50;\n  var CREEP_SPAWN_TIME = 3;\n  var RESOURCE_ENERGY = \"energy\";\n  var RESOURCES_ALL = [RESOURCE_ENERGY];\n  var SOURCE_ENERGY_REGEN = 10;\n  var RESOURCE_DECAY = 1e3;\n  var SOURCE_ENERGY_CAPACITY = 3e3;\n  var MAX_CONSTRUCTION_SITES = 10;\n  var CONSTRUCTION_COST = {\n    spawn: 15e3,\n    extension: 3e3,\n    road: 300,\n    constructedWall: 1,\n    rampart: 1,\n    container: 5e3,\n    tower: 5e3\n  };\n  var CONSTRUCTION_COST_ROAD_SWAMP_RATIO = 5;\n  var CONSTRUCTION_COST_ROAD_WALL_RATIO = 150;\n  var CONTAINER_HITS = 300;\n  var CONTAINER_CAPACITY = 2e3;\n  var WALL_HITS = 1e4;\n  var WALL_HITS_MAX = 1e4;\n  var RAMPART_HITS = 1e4;\n  var RAMPART_HITS_MAX = 1e4;\n  var ROAD_HITS = 500;\n  var ROAD_WEAROUT = 1;\n  var EXTENSION_HITS = 100;\n  var EXTENSION_ENERGY_CAPACITY = 100;\n  var SPAWN_ENERGY_CAPACITY = 1e3;\n  var SPAWN_HITS = 3e3;\n  var PLAYER_1 = \"player1\";\n  var PLAYER_2 = \"player2\";\n  var PLAYER_NEUTRAL = \"neutral\";\n  var PLAYER_COLORS = {\n    [PLAYER_1]: 3577332,\n    // Blue\n    [PLAYER_2]: 16007990,\n    // Red\n    [PLAYER_NEUTRAL]: 8947848\n    // Gray\n  };\n  var PLAYER_COLORS_CSS = {\n    [PLAYER_1]: \"#3695F4\",\n    // Blue\n    [PLAYER_2]: \"#F44336\",\n    // Red\n    [PLAYER_NEUTRAL]: \"#888888\"\n    // Gray\n  };\n  var EVENT_ATTACK = 1;\n  var EVENT_OBJECT_DESTROYED = 2;\n  var EVENT_ATTACK_CONTROLLER = 3;\n  var EVENT_BUILD = 4;\n  var EVENT_HARVEST = 5;\n  var EVENT_HEAL = 6;\n  var EVENT_RESERVE_CONTROLLER = 8;\n  var EVENT_UPGRADE_CONTROLLER = 9;\n  var EVENT_EXIT = 10;\n  var EVENT_POWER = 11;\n  var EVENT_TRANSFER = 12;\n  var EVENT_WITHDRAW = 13;\n  var EVENT_PICKUP = 14;\n  var EVENT_DROP = 15;\n  var EVENT_PULL = 16;\n  var EVENT_DESTROY = 18;\n  var EVENT_CREATE_CREEP = 19;\n  var C = {\n    OK,\n    ERR_NOT_OWNER,\n    ERR_NO_PATH,\n    ERR_NAME_EXISTS,\n    ERR_BUSY,\n    ERR_NOT_FOUND,\n    ERR_NOT_ENOUGH_ENERGY,\n    ERR_NOT_ENOUGH_RESOURCES,\n    ERR_INVALID_TARGET,\n    ERR_FULL,\n    ERR_NOT_IN_RANGE,\n    ERR_INVALID_ARGS,\n    ERR_TIRED,\n    ERR_NO_BODYPART,\n    ERR_NOT_ENOUGH_EXTENSIONS,\n    MOVE,\n    RANGED_ATTACK,\n    HEAL,\n    ATTACK,\n    CARRY,\n    TOUGH,\n    WORK,\n    TOP,\n    TOP_RIGHT,\n    RIGHT,\n    BOTTOM_RIGHT,\n    BOTTOM,\n    BOTTOM_LEFT,\n    LEFT,\n    TOP_LEFT,\n    TERRAIN_PLAIN,\n    TERRAIN_WALL,\n    TERRAIN_SWAMP,\n    // Structure types\n    STRUCTURE_SPAWN,\n    STRUCTURE_EXTENSION,\n    STRUCTURE_ROAD,\n    STRUCTURE_WALL,\n    STRUCTURE_RAMPART,\n    STRUCTURE_KEEPER_LAIR,\n    STRUCTURE_PORTAL,\n    STRUCTURE_CONTROLLER,\n    STRUCTURE_LINK,\n    STRUCTURE_STORAGE,\n    STRUCTURE_TOWER,\n    STRUCTURE_OBSERVER,\n    STRUCTURE_POWER_BANK,\n    STRUCTURE_POWER_SPAWN,\n    STRUCTURE_EXTRACTOR,\n    STRUCTURE_LAB,\n    STRUCTURE_TERMINAL,\n    STRUCTURE_CONTAINER,\n    STRUCTURE_NUKER,\n    STRUCTURE_FACTORY,\n    STRUCTURE_INVADER_CORE,\n    STRUCTURE_CREEP,\n    STRUCTURE_CONSTRUCTED_WALL,\n    SOURCE,\n    RESOURCE,\n    CONSTRUCTION_SITE,\n    // Combat stats\n    BODYPART_HITS,\n    RANGED_ATTACK_POWER,\n    RANGED_ATTACK_DISTANCE_RATE,\n    ATTACK_POWER,\n    HEAL_POWER,\n    RANGED_HEAL_POWER,\n    CARRY_CAPACITY,\n    HARVEST_POWER,\n    BUILD_POWER,\n    OBSTACLE_OBJECT_TYPES,\n    TOWER_ENERGY_COST,\n    TOWER_RANGE,\n    TOWER_HITS,\n    TOWER_CAPACITY,\n    TOWER_POWER_ATTACK,\n    TOWER_POWER_HEAL,\n    TOWER_OPTIMAL_RANGE,\n    TOWER_FALLOFF_RANGE,\n    TOWER_FALLOFF,\n    TOWER_COOLDOWN,\n    BODYPART_COST,\n    MAX_CREEP_SIZE,\n    CREEP_SPAWN_TIME,\n    RESOURCE_ENERGY,\n    RESOURCES_ALL,\n    SOURCE_ENERGY_REGEN,\n    SOURCE_ENERGY_CAPACITY,\n    RESOURCE_DECAY,\n    MAX_CONSTRUCTION_SITES,\n    CONSTRUCTION_COST,\n    CONSTRUCTION_COST_ROAD_SWAMP_RATIO,\n    CONSTRUCTION_COST_ROAD_WALL_RATIO,\n    CONTAINER_HITS,\n    CONTAINER_CAPACITY,\n    WALL_HITS,\n    WALL_HITS_MAX,\n    RAMPART_HITS,\n    RAMPART_HITS_MAX,\n    ROAD_HITS,\n    ROAD_WEAROUT,\n    EXTENSION_HITS,\n    EXTENSION_ENERGY_CAPACITY,\n    SPAWN_ENERGY_CAPACITY,\n    SPAWN_HITS,\n    // Events\n    EVENT_ATTACK,\n    EVENT_OBJECT_DESTROYED,\n    EVENT_ATTACK_CONTROLLER,\n    EVENT_BUILD,\n    EVENT_HARVEST,\n    EVENT_HEAL,\n    EVENT_RESERVE_CONTROLLER,\n    EVENT_UPGRADE_CONTROLLER,\n    EVENT_EXIT,\n    EVENT_POWER,\n    EVENT_TRANSFER,\n    EVENT_WITHDRAW,\n    EVENT_PICKUP,\n    EVENT_DROP,\n    EVENT_PULL,\n    EVENT_DESTROY,\n    EVENT_CREATE_CREEP,\n    // Players\n    PLAYER_1,\n    PLAYER_2,\n    PLAYER_NEUTRAL,\n    PLAYER_COLORS,\n    PLAYER_COLORS_CSS\n  };\n\n  // ../maps/dist/index.js\n  var DEFAULT_MAP_SIZE = 100;\n\n  // src/runner/sandbox-runtime.ts\n  function getObjectsFromHost() {\n    if (typeof _getObjectsDirect === \"function\") {\n      return _getObjectsDirect();\n    }\n    return _getObjects.applySync(void 0, []);\n  }\n  function addIntentToHost(objectId, type, data) {\n    if (typeof _addIntentDirect === \"function\") {\n      _addIntentDirect(objectId, type, data);\n      return;\n    }\n    _addIntent.applySync(void 0, [objectId, type, data]);\n  }\n  function consoleLogToHost(msg) {\n    if (typeof _consoleLogDirect === \"function\") {\n      _consoleLogDirect(msg);\n      return;\n    }\n    _consoleLog.applySync(void 0, [msg]);\n  }\n  function generateIdFromHost() {\n    if (typeof _generateIdDirect === \"function\") {\n      return _generateIdDirect();\n    }\n    return _generateId.applySync(void 0, []);\n  }\n  var ATTACK2 = C.ATTACK;\n  var RANGED_ATTACK2 = C.RANGED_ATTACK;\n  var HEAL2 = C.HEAL;\n  var MOVE2 = C.MOVE;\n  var WORK2 = C.WORK;\n  var CARRY2 = C.CARRY;\n  var TOUGH2 = C.TOUGH;\n  var TOP2 = C.TOP;\n  var TOP_RIGHT2 = C.TOP_RIGHT;\n  var RIGHT2 = C.RIGHT;\n  var BOTTOM_RIGHT2 = C.BOTTOM_RIGHT;\n  var BOTTOM2 = C.BOTTOM;\n  var BOTTOM_LEFT2 = C.BOTTOM_LEFT;\n  var LEFT2 = C.LEFT;\n  var TOP_LEFT2 = C.TOP_LEFT;\n  var TERRAIN_PLAIN2 = C.TERRAIN_PLAIN;\n  var TERRAIN_WALL2 = C.TERRAIN_WALL;\n  var TERRAIN_SWAMP2 = C.TERRAIN_SWAMP;\n  var RESOURCE_ENERGY2 = C.RESOURCE_ENERGY;\n  var OK2 = C.OK;\n  var ERR_NOT_OWNER2 = C.ERR_NOT_OWNER;\n  var ERR_NO_PATH2 = C.ERR_NO_PATH;\n  var ERR_BUSY2 = C.ERR_BUSY;\n  var ERR_NOT_FOUND2 = C.ERR_NOT_FOUND;\n  var ERR_NOT_ENOUGH_RESOURCES2 = C.ERR_NOT_ENOUGH_RESOURCES;\n  var ERR_INVALID_TARGET2 = C.ERR_INVALID_TARGET;\n  var ERR_FULL2 = C.ERR_FULL;\n  var ERR_NOT_IN_RANGE2 = C.ERR_NOT_IN_RANGE;\n  var ERR_INVALID_ARGS2 = C.ERR_INVALID_ARGS;\n  var ERR_TIRED2 = C.ERR_TIRED;\n  var ERR_NO_BODYPART2 = C.ERR_NO_BODYPART;\n  var CARRY_CAPACITY2 = C.CARRY_CAPACITY;\n  var SPAWN_ENERGY_CAPACITY2 = C.SPAWN_ENERGY_CAPACITY;\n  var TOWER_CAPACITY2 = C.TOWER_CAPACITY;\n  var SOURCE_ENERGY_CAPACITY2 = C.SOURCE_ENERGY_CAPACITY;\n  var ARENA_SIZE = DEFAULT_MAP_SIZE;\n  var Creep = class {\n  };\n  var StructureSpawn = class {\n  };\n  var StructureTower = class {\n  };\n  var Source = class {\n  };\n  var Resource = class {\n  };\n  var ConstructionSite = class {\n  };\n  var _objectCache = /* @__PURE__ */ new Map();\n  var _terrainCache = null;\n  var _currentTick = 0;\n  function _getTerrainAtCached(x, y) {\n    if (!_terrainCache)\n      return TERRAIN_PLAIN2;\n    if (x < 0 || x >= ARENA_SIZE || y < 0 || y >= ARENA_SIZE)\n      return TERRAIN_WALL2;\n    return _terrainCache[y * ARENA_SIZE + x] || TERRAIN_PLAIN2;\n  }\n  function getDirection(dx, dy) {\n    if (dx === 0 && dy < 0)\n      return TOP2;\n    if (dx > 0 && dy < 0)\n      return TOP_RIGHT2;\n    if (dx > 0 && dy === 0)\n      return RIGHT2;\n    if (dx > 0 && dy > 0)\n      return BOTTOM_RIGHT2;\n    if (dx === 0 && dy > 0)\n      return BOTTOM2;\n    if (dx < 0 && dy > 0)\n      return BOTTOM_LEFT2;\n    if (dx < 0 && dy === 0)\n      return LEFT2;\n    if (dx < 0 && dy < 0)\n      return TOP_LEFT2;\n    return TOP2;\n  }\n  function getRange2(pos1, pos2) {\n    return Math.max(Math.abs(pos1.x - pos2.x), Math.abs(pos1.y - pos2.y));\n  }\n  function findClosestByRange2(pos, objects) {\n    let closest = null;\n    let minRange = Infinity;\n    for (const obj of objects) {\n      const range = getRange2(pos, obj);\n      if (range < minRange) {\n        minRange = range;\n        closest = obj;\n      }\n    }\n    return closest;\n  }\n  function findInRange2(pos, objects, range) {\n    return objects.filter((obj) => getRange2(pos, obj) <= range);\n  }\n  function isWalkable(x, y) {\n    if (x < 0 || x >= ARENA_SIZE || y < 0 || y >= ARENA_SIZE)\n      return false;\n    return _getTerrainAtCached(x, y) !== TERRAIN_WALL2;\n  }\n  function hasBodyPart(body, partType) {\n    return body !== void 0 && body.some((p) => p.type === partType && p.hits > 0);\n  }\n  function searchPath2(origin, goalInput, opts) {\n    const goals = Array.isArray(goalInput) ? goalInput : [goalInput];\n    const plainCost = opts?.plainCost ?? 1;\n    const swampCost = opts?.swampCost ?? 5;\n    const goalSet = /* @__PURE__ */ new Set();\n    for (const g of goals) {\n      const range = g.range ?? 0;\n      for (let dx = -range; dx <= range; dx++) {\n        for (let dy = -range; dy <= range; dy++) {\n          const gx = g.pos.x + dx;\n          const gy = g.pos.y + dy;\n          if (gx >= 0 && gx < ARENA_SIZE && gy >= 0 && gy < ARENA_SIZE) {\n            if (Math.max(Math.abs(dx), Math.abs(dy)) <= range) {\n              goalSet.add(gx + \",\" + gy);\n            }\n          }\n        }\n      }\n    }\n    if (goalSet.has(origin.x + \",\" + origin.y)) {\n      return { path: [], ops: 0, cost: 0, incomplete: false };\n    }\n    const openSet = [{ x: origin.x, y: origin.y, g: 0, h: 0, parent: null }];\n    const closedSet = /* @__PURE__ */ new Set();\n    const gScores = /* @__PURE__ */ new Map();\n    const heuristic = (x, y) => {\n      let minDist = Infinity;\n      for (const g of goals) {\n        const dist = Math.max(Math.abs(x - g.pos.x), Math.abs(y - g.pos.y));\n        minDist = Math.min(minDist, Math.max(0, dist - (g.range ?? 0)));\n      }\n      return minDist;\n    };\n    gScores.set(origin.x + \",\" + origin.y, 0);\n    const directions = [\n      { dx: 0, dy: -1 },\n      { dx: 1, dy: -1 },\n      { dx: 1, dy: 0 },\n      { dx: 1, dy: 1 },\n      { dx: 0, dy: 1 },\n      { dx: -1, dy: 1 },\n      { dx: -1, dy: 0 },\n      { dx: -1, dy: -1 }\n    ];\n    let ops = 0;\n    const maxOps = 2e3;\n    while (openSet.length > 0 && ops < maxOps) {\n      ops++;\n      openSet.sort((a, b) => a.g + a.h - (b.g + b.h));\n      const current = openSet.shift();\n      const currentKey = current.x + \",\" + current.y;\n      if (goalSet.has(currentKey)) {\n        const path = [];\n        let node = current;\n        while (node && node.parent) {\n          path.unshift({ x: node.x, y: node.y });\n          node = node.parent;\n        }\n        return { path, ops, cost: current.g, incomplete: false };\n      }\n      closedSet.add(currentKey);\n      for (const dir of directions) {\n        const nx = current.x + dir.dx;\n        const ny = current.y + dir.dy;\n        const neighborKey = nx + \",\" + ny;\n        if (closedSet.has(neighborKey))\n          continue;\n        if (!isWalkable(nx, ny))\n          continue;\n        let moveCost = plainCost;\n        const costMatrix = opts?.costMatrix;\n        if (costMatrix) {\n          const cmCost = costMatrix.get(nx, ny);\n          if (cmCost === 255)\n            continue;\n          if (cmCost > 0)\n            moveCost = cmCost;\n        } else {\n          const terrain = _getTerrainAtCached(nx, ny);\n          if (terrain === TERRAIN_SWAMP2)\n            moveCost = swampCost;\n        }\n        const tentativeG = current.g + moveCost;\n        const existingG = gScores.get(neighborKey);\n        if (existingG === void 0 || tentativeG < existingG) {\n          gScores.set(neighborKey, tentativeG);\n          const neighborNode = {\n            x: nx,\n            y: ny,\n            g: tentativeG,\n            h: heuristic(nx, ny),\n            parent: current\n          };\n          const existingIdx = openSet.findIndex((n) => n.x === nx && n.y === ny);\n          if (existingIdx >= 0) {\n            openSet[existingIdx] = neighborNode;\n          } else {\n            openSet.push(neighborNode);\n          }\n        }\n      }\n    }\n    return { path: [], ops, cost: 0, incomplete: true };\n  }\n  function findPath2(from, to, opts) {\n    const range = opts?.range ?? 0;\n    const result = searchPath2(from, [{ pos: to, range }], opts);\n    return result.path;\n  }\n  function findClosestByPath2(pos, objects, opts) {\n    let closest = null;\n    let shortestCost = Infinity;\n    const range = opts?.range ?? 0;\n    for (const obj of objects) {\n      const dist = getRange2(pos, obj);\n      if (dist <= range) {\n        return obj;\n      }\n      const result = searchPath2(pos, [{ pos: obj, range }], opts);\n      if (!result.incomplete && result.cost < shortestCost) {\n        shortestCost = result.cost;\n        closest = obj;\n      }\n    }\n    return closest;\n  }\n  function createStore(storeData, objectType, body) {\n    const data = storeData || {};\n    let capacity = 0;\n    if (objectType === \"creep\" && body) {\n      capacity = body.filter((p) => p.type === CARRY2).length * CARRY_CAPACITY2;\n    } else if (objectType === \"spawn\") {\n      capacity = SPAWN_ENERGY_CAPACITY2;\n    } else if (objectType === \"tower\") {\n      capacity = TOWER_CAPACITY2;\n    }\n    return {\n      _data: data,\n      _capacity: capacity,\n      get energy() {\n        return this._data.energy || 0;\n      },\n      getCapacity(resource) {\n        if (resource !== void 0) {\n          return resource === RESOURCE_ENERGY2 ? this._capacity : null;\n        }\n        return this._capacity;\n      },\n      getUsedCapacity(resource) {\n        if (resource !== void 0) {\n          return this._data[resource] || 0;\n        }\n        return Object.values(this._data).reduce((a, b) => a + (typeof b === \"number\" ? b : 0), 0);\n      },\n      getFreeCapacity(resource) {\n        if (resource !== void 0) {\n          if (resource !== RESOURCE_ENERGY2)\n            return null;\n          return this._capacity - (this._data[resource] || 0);\n        }\n        const used = Object.values(this._data).reduce((a, b) => a + (typeof b === \"number\" ? b : 0), 0);\n        return this._capacity - used;\n      },\n      _update(newData, newBody) {\n        this._data = newData || {};\n        if (newBody) {\n          this._capacity = newBody.filter((p) => p.type === CARRY2).length * CARRY_CAPACITY2;\n        }\n      }\n    };\n  }\n  function addIntent(objectId, type, data) {\n    addIntentToHost(objectId, type, JSON.stringify(data));\n  }\n  var _prototypeMap = {\n    \"creep\": Creep,\n    \"spawn\": StructureSpawn,\n    \"tower\": StructureTower,\n    \"source\": Source,\n    \"energy\": Resource,\n    \"resource\": Resource,\n    \"constructionSite\": ConstructionSite\n  };\n  function _updateWrapper(wrapper, data) {\n    wrapper.x = data.x;\n    wrapper.y = data.y;\n    wrapper.exists = true;\n    if (data.type === \"creep\") {\n      wrapper.hits = data.hits;\n      wrapper.hitsMax = data.hitsMax;\n      wrapper.fatigue = data.fatigue;\n      wrapper.body = data.body;\n      wrapper.spawning = data.spawning;\n      if (wrapper.store)\n        wrapper.store._update(data.store || {}, data.body);\n    } else if (data.type === \"spawn\") {\n      wrapper.hits = data.hits;\n      wrapper.hitsMax = data.hitsMax;\n      wrapper.spawning = data.spawning;\n      if (wrapper.store)\n        wrapper.store._update(data.store || {});\n    } else if (data.type === \"tower\") {\n      wrapper.hits = data.hits;\n      wrapper.hitsMax = data.hitsMax;\n      wrapper.cooldown = data.cooldown;\n      if (wrapper.store)\n        wrapper.store._update(data.store || {});\n    } else if (data.type === \"source\") {\n      wrapper.energy = data.energy;\n    } else if (data.type === \"energy\" || data.type === \"resource\") {\n      wrapper.amount = data.amount;\n    } else if (data.type === \"constructionSite\") {\n      wrapper.progress = data.progress;\n      wrapper.progressTotal = data.progressTotal;\n    }\n  }\n  function _createWrapper(data) {\n    const isMy = data.user === _playerId;\n    const obj = {\n      id: data.id,\n      x: data.x,\n      y: data.y,\n      exists: true,\n      _type: data.type,\n      user: data.user,\n      my: isMy\n    };\n    if (_prototypeMap[data.type]) {\n      Object.setPrototypeOf(obj, _prototypeMap[data.type].prototype);\n    }\n    obj.getRangeTo = function(target) {\n      const pos = \"pos\" in target ? target.pos : target;\n      return getRange2(this, pos);\n    };\n    obj.findClosestByRange = function(objects) {\n      return findClosestByRange2(this, objects);\n    };\n    obj.findInRange = function(objects, range) {\n      return findInRange2(this, objects, range);\n    };\n    obj.findPathTo = function(target, opts) {\n      return findPath2(this, target, opts);\n    };\n    obj.findClosestByPath = function(objects, opts) {\n      return findClosestByPath2(this, objects, opts);\n    };\n    if (data.type === \"creep\") {\n      obj.hits = data.hits;\n      obj.hitsMax = data.hitsMax;\n      obj.fatigue = data.fatigue;\n      obj.body = data.body;\n      obj.spawning = data.spawning;\n      obj.store = createStore(data.store, \"creep\", data.body);\n      obj.move = function(direction) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        addIntent(this.id, \"move\", { direction });\n        return OK2;\n      };\n      obj.moveTo = function(target, opts) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        if (!this.exists)\n          return ERR_NOT_FOUND2;\n        const range = opts?.range ?? 0;\n        const currentRange = getRange2(this, target);\n        if (currentRange <= range)\n          return OK2;\n        const path = findPath2({ x: this.x, y: this.y }, target, { range });\n        if (path.length === 0)\n          return ERR_NO_PATH2;\n        const next = path[0];\n        const dx = next.x - this.x;\n        const dy = next.y - this.y;\n        if (dx === 0 && dy === 0)\n          return OK2;\n        addIntent(this.id, \"move\", { direction: getDirection(dx, dy) });\n        return OK2;\n      };\n      obj.attack = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        if (!this.exists)\n          return ERR_NOT_FOUND2;\n        if (!hasBodyPart(this.body, ATTACK2))\n          return ERR_NO_BODYPART2;\n        if (target.x !== void 0 && target.y !== void 0) {\n          if (getRange2(this, target) > 1)\n            return ERR_NOT_IN_RANGE2;\n        }\n        addIntent(this.id, \"attack\", { id: target.id });\n        return OK2;\n      };\n      obj.rangedAttack = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        if (!this.exists)\n          return ERR_NOT_FOUND2;\n        if (!hasBodyPart(this.body, RANGED_ATTACK2))\n          return ERR_NO_BODYPART2;\n        if (target.x !== void 0 && target.y !== void 0) {\n          if (getRange2(this, target) > 3)\n            return ERR_NOT_IN_RANGE2;\n        }\n        addIntent(this.id, \"rangedAttack\", { id: target.id });\n        return OK2;\n      };\n      obj.rangedMassAttack = function() {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        if (!this.exists)\n          return ERR_NOT_FOUND2;\n        if (!hasBodyPart(this.body, RANGED_ATTACK2))\n          return ERR_NO_BODYPART2;\n        addIntent(this.id, \"rangedMassAttack\", {});\n        return OK2;\n      };\n      obj.heal = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        if (!this.exists)\n          return ERR_NOT_FOUND2;\n        if (!hasBodyPart(this.body, HEAL2))\n          return ERR_NO_BODYPART2;\n        if (target.x !== void 0 && target.y !== void 0) {\n          if (getRange2(this, target) > 1)\n            return ERR_NOT_IN_RANGE2;\n        }\n        addIntent(this.id, \"heal\", { id: target.id });\n        return OK2;\n      };\n      obj.rangedHeal = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        if (!this.exists)\n          return ERR_NOT_FOUND2;\n        if (!hasBodyPart(this.body, HEAL2))\n          return ERR_NO_BODYPART2;\n        if (target.x !== void 0 && target.y !== void 0) {\n          if (getRange2(this, target) > 3)\n            return ERR_NOT_IN_RANGE2;\n        }\n        addIntent(this.id, \"rangedHeal\", { id: target.id });\n        return OK2;\n      };\n      obj.harvest = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        if (!this.exists)\n          return ERR_NOT_FOUND2;\n        if (!hasBodyPart(this.body, WORK2))\n          return ERR_NO_BODYPART2;\n        if (target.x !== void 0 && target.y !== void 0) {\n          if (getRange2(this, target) > 1)\n            return ERR_NOT_IN_RANGE2;\n        }\n        addIntent(this.id, \"harvest\", { id: target.id });\n        return OK2;\n      };\n      obj.build = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        if (!this.exists)\n          return ERR_NOT_FOUND2;\n        if (!hasBodyPart(this.body, WORK2))\n          return ERR_NO_BODYPART2;\n        if (target.x !== void 0 && target.y !== void 0) {\n          if (getRange2(this, target) > 3)\n            return ERR_NOT_IN_RANGE2;\n        }\n        addIntent(this.id, \"build\", { id: target.id });\n        return OK2;\n      };\n      obj.transfer = function(target, resourceType, amount) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        addIntent(this.id, \"transfer\", { id: target.id, resourceType, amount });\n        return OK2;\n      };\n      obj.withdraw = function(target, resourceType, amount) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        addIntent(this.id, \"withdraw\", { id: target.id, resourceType, amount });\n        return OK2;\n      };\n      obj.drop = function(resourceType, amount) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        addIntent(this.id, \"drop\", { resourceType, amount });\n        return OK2;\n      };\n      obj.pickup = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        addIntent(this.id, \"pickup\", { id: target.id });\n        return OK2;\n      };\n      obj.pull = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        addIntent(this.id, \"pull\", { id: target.id });\n        return OK2;\n      };\n    } else if (data.type === \"spawn\") {\n      obj.hits = data.hits;\n      obj.hitsMax = data.hitsMax;\n      obj.spawning = data.spawning;\n      obj.store = createStore(data.store, \"spawn\");\n      obj.spawnCreep = function(body) {\n        if (!isMy)\n          return { error: ERR_NOT_OWNER2 };\n        addIntent(this.id, \"spawnCreep\", { body });\n        return { object: null };\n      };\n    } else if (data.type === \"tower\") {\n      obj.hits = data.hits;\n      obj.hitsMax = data.hitsMax;\n      obj.cooldown = data.cooldown;\n      obj.store = createStore(data.store, \"tower\");\n      obj.attack = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        addIntent(this.id, \"towerAttack\", { id: target.id });\n        return OK2;\n      };\n      obj.heal = function(target) {\n        if (!isMy)\n          return ERR_NOT_OWNER2;\n        addIntent(this.id, \"towerHeal\", { id: target.id });\n        return OK2;\n      };\n    } else if (data.type === \"source\") {\n      obj.energy = data.energy;\n      obj.energyCapacity = SOURCE_ENERGY_CAPACITY2;\n    } else if (data.type === \"energy\" || data.type === \"resource\") {\n      obj.amount = data.amount;\n      obj.resourceType = RESOURCE_ENERGY2;\n    } else if (data.type === \"constructionSite\") {\n      obj.progress = data.progress;\n      obj.progressTotal = data.progressTotal;\n      obj.structureType = data.structureType;\n    }\n    return obj;\n  }\n  function _refreshObjectCache() {\n    const rawObjects = JSON.parse(getObjectsFromHost());\n    const currentIds = new Set(rawObjects.map((o) => o.id));\n    for (const [id, wrapper] of _objectCache) {\n      if (!currentIds.has(id)) {\n        wrapper.exists = false;\n      }\n    }\n    for (const data of rawObjects) {\n      let wrapper = _objectCache.get(data.id);\n      if (wrapper) {\n        _updateWrapper(wrapper, data);\n      } else {\n        wrapper = _createWrapper(data);\n        _objectCache.set(data.id, wrapper);\n      }\n    }\n  }\n  function _refreshTick(terrainArray, tickNumber) {\n    if (terrainArray && terrainArray.length > 0) {\n      _terrainCache = new Uint8Array(terrainArray);\n    }\n    _currentTick = tickNumber;\n    _refreshObjectCache();\n  }\n  var CostMatrix = class _CostMatrix {\n    constructor() {\n      __publicField(this, \"_bits\");\n      this._bits = new Uint8Array(ARENA_SIZE * ARENA_SIZE);\n    }\n    set(x, y, value) {\n      this._bits[y * ARENA_SIZE + x] = value;\n    }\n    get(x, y) {\n      return this._bits[y * ARENA_SIZE + x];\n    }\n    clone() {\n      const cm = new _CostMatrix();\n      cm._bits.set(this._bits);\n      return cm;\n    }\n  };\n  var Visual = class {\n    constructor(layer = 0, persistent = false) {\n      __publicField(this, \"layer\");\n      __publicField(this, \"persistent\");\n      this.layer = layer;\n      this.persistent = persistent;\n    }\n    circle(_pos, _style) {\n      return this;\n    }\n    line(_pos1, _pos2, _style) {\n      return this;\n    }\n    rect(_pos, _w, _h, _style) {\n      return this;\n    }\n    poly(_points, _style) {\n      return this;\n    }\n    text(_text, _pos, _style) {\n      return this;\n    }\n    clear() {\n      return this;\n    }\n    get size() {\n      return 0;\n    }\n  };\n  var console = {\n    log: (...args) => consoleLogToHost(args.map((a) => String(a)).join(\" \"))\n  };\n  function getObjectsByPrototype(prototype) {\n    const results = [];\n    for (const [, wrapper] of _objectCache) {\n      if (!wrapper.exists)\n        continue;\n      let matches = false;\n      if (prototype === Creep && wrapper._type === \"creep\")\n        matches = true;\n      if (prototype === StructureSpawn && wrapper._type === \"spawn\")\n        matches = true;\n      if (prototype === StructureTower && wrapper._type === \"tower\")\n        matches = true;\n      if (prototype === Source && wrapper._type === \"source\")\n        matches = true;\n      if (prototype === Resource && (wrapper._type === \"energy\" || wrapper._type === \"resource\"))\n        matches = true;\n      if (prototype === ConstructionSite && wrapper._type === \"constructionSite\")\n        matches = true;\n      if (matches)\n        results.push(wrapper);\n    }\n    return results;\n  }\n  function getObjects() {\n    return Array.from(_objectCache.values()).filter((w) => w.exists);\n  }\n  function getObjectById(id) {\n    const wrapper = _objectCache.get(id);\n    return wrapper && wrapper.exists ? wrapper : null;\n  }\n  function getTicks() {\n    return _currentTick;\n  }\n  function getTerrainAt(pos) {\n    return _getTerrainAtCached(pos.x, pos.y);\n  }\n  function createConstructionSite(pos, structureType) {\n    const id = generateIdFromHost();\n    addIntent(\"global\", \"createConstructionSite\", { x: pos.x, y: pos.y, structureType, id });\n    return { object: null };\n  }\n  function getCpuTime() {\n    return 0;\n  }\n  var _global = typeof globalThis !== \"undefined\" ? globalThis : typeof global !== \"undefined\" ? global : typeof self !== \"undefined\" ? self : {};\n  Object.assign(_global, {\n    // Prototypes\n    Creep,\n    StructureSpawn,\n    StructureTower,\n    Source,\n    Resource,\n    ConstructionSite,\n    // Classes\n    CostMatrix,\n    Visual,\n    // Constants\n    ATTACK: ATTACK2,\n    RANGED_ATTACK: RANGED_ATTACK2,\n    HEAL: HEAL2,\n    MOVE: MOVE2,\n    WORK: WORK2,\n    CARRY: CARRY2,\n    TOUGH: TOUGH2,\n    TOP: TOP2,\n    TOP_RIGHT: TOP_RIGHT2,\n    RIGHT: RIGHT2,\n    BOTTOM_RIGHT: BOTTOM_RIGHT2,\n    BOTTOM: BOTTOM2,\n    BOTTOM_LEFT: BOTTOM_LEFT2,\n    LEFT: LEFT2,\n    TOP_LEFT: TOP_LEFT2,\n    TERRAIN_PLAIN: TERRAIN_PLAIN2,\n    TERRAIN_WALL: TERRAIN_WALL2,\n    TERRAIN_SWAMP: TERRAIN_SWAMP2,\n    RESOURCE_ENERGY: RESOURCE_ENERGY2,\n    OK: OK2,\n    ERR_NOT_OWNER: ERR_NOT_OWNER2,\n    ERR_NO_PATH: ERR_NO_PATH2,\n    ERR_BUSY: ERR_BUSY2,\n    ERR_NOT_FOUND: ERR_NOT_FOUND2,\n    ERR_NOT_ENOUGH_RESOURCES: ERR_NOT_ENOUGH_RESOURCES2,\n    ERR_INVALID_TARGET: ERR_INVALID_TARGET2,\n    ERR_FULL: ERR_FULL2,\n    ERR_NOT_IN_RANGE: ERR_NOT_IN_RANGE2,\n    ERR_INVALID_ARGS: ERR_INVALID_ARGS2,\n    ERR_TIRED: ERR_TIRED2,\n    ERR_NO_BODYPART: ERR_NO_BODYPART2,\n    // Functions\n    getObjectsByPrototype,\n    getObjects,\n    getObjectById,\n    getTicks,\n    getTerrainAt,\n    getDirection,\n    getRange: getRange2,\n    findClosestByRange: findClosestByRange2,\n    findInRange: findInRange2,\n    findPath: findPath2,\n    findClosestByPath: findClosestByPath2,\n    searchPath: searchPath2,\n    createConstructionSite,\n    getCpuTime,\n    console,\n    // Internal (called by host before each loop)\n    _refreshTick,\n    _refreshObjectCache\n  });\n})();\n";
