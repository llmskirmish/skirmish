/**
 * Position interface - base for all game objects with coordinates
 */
export interface Position {
  /** The X coordinate in the arena */
  x: number;
  /** The Y coordinate in the arena */
  y: number;
}

